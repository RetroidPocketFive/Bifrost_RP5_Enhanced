<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!-- Keep the LED pipeline warm while a PIPBOY override is active so the
         first action after idle isn't delayed by CPU power-down. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <!-- Plugin store: fetch the catalogue + download bundles. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="${applicationId}.permission.CONTROL_LEDS"
        android:label="@string/perm_control_leds_label"
        android:description="@string/perm_control_leds_desc"
        android:protectionLevel="normal" />

    <uses-permission android:name="${applicationId}.permission.CONTROL_LEDS" />


    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".BifrostApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Bifrost">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-run guide launched from MainActivity.onCreate. Must be
             declared or R8 (minifyEnabled) renames the class and startActivity
             fails with ActivityNotFoundException, crashing app launch. -->
        <activity
            android:name=".rp5.Rp5CalibrationActivity"
            android:exported="false"
            android:label="RP5 Live Calibration" />

        <activity
            android:name=".StartupGuideActivity"
            android:exported="false" />

        <activity
            android:name=".plugins.PluginStoreActivity"
            android:exported="false"
            android:label="Plugin Store" />

        <activity
            android:name=".schedule.ScheduleActivity"
            android:exported="false"
            android:label="Schedule" />

        <service
            android:name=".services.LEDService"
            android:foregroundServiceType="mediaProjection|specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent LED control with accessibility-based screen capture" />
        </service>

        <service
            android:name=".services.BifrostAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".services.VideoLiveWallpaperService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/live_wallpaper" />
        </service>

        <service
            android:name=".services.BifrostTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification_small"
            android:label="Bifrost"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.ScheduleReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.moonbench.bifrost.action.SCHEDULE_TICK" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".external.ExternalApiReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="${applicationId}.permission.CONTROL_LEDS">
            <intent-filter>
                <action android:name="com.moonbench.bifrost.api.ACTION_DISPLAY" />
                <action android:name="com.moonbench.bifrost.api.ACTION_CLEAR" />
                <action android:name="com.moonbench.bifrost.api.ACTION_INSTALL_PROFILE" />
                <action android:name="com.moonbench.bifrost.api.ACTION_UNINSTALL_PROFILE" />
                <action android:name="com.moonbench.bifrost.api.ACTION_QUERY_PLUGIN" />
                <action android:name="com.moonbench.bifrost.api.ACTION_PULSE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.PackageRemovedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>